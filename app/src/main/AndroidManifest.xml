<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AulaFood"
        tools:targetApi="31" >
        <activity
            android:name=".MultiFileUploadMainActivity"
            android:exported="false"
            android:label="MultiFileUploadMainActivity"
            android:theme="@style/Theme.AulaFood" />
        <activity
            android:name=".DocumentActivity"
            android:exported="false"
            android:label="DocumentActivity"
            android:theme="@style/Theme.AulaFood" />
        <activity
            android:name=".CadastroActivity"
            android:exported="false"
            android:label="CadastroActivity"
            android:theme="@style/Theme.AulaFood" />
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:label="LoginActivity"
            android:theme="@style/Theme.AulaFood" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="IntoActivity"
            android:theme="@style/Theme.AulaFood" />
        <activity
            android:name=".IntoActivity"
            android:exported="false"
            android:label="AulaFood"
            android:theme="@style/Theme.AulaFood" />
        <activity
            android:name=".MultipleImagesActivity"
            android:exported="true"
            android:label="MultipleImagesActivity"
            android:theme="@style/Theme.AulaFood" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>